$().ready(function() {
    /*loginMsg('${msg}');

    $("#signIn").on("click", function() {
        // TODO
        var formData = new Object();
        var tempData = $("form").serializeArray();
        for(var i = 0; i < tempData.length; i++) {
            var data = tempData[i];
            if(data.name == "accountName") {
                formData.accountName = data.value;
            }else {
                formData.password = data.value;
            }
        }

        POST("/user/login", formData, function(data) {
            console.log(data.retData)
            window.location = document.location.origin + data.retData;
        });
    })*/
});